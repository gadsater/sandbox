set number
let mapleader = "-"
let maplocalleader = "\\"

call plug#begin()
Plug 'tpope/vim-sensible'
Plug 'vim-scripts/paredit.vim'
Plug 'tpope/vim-fugitive'
Plug 'vim-airline/vim-airline'
call plug#end()

nnoremap <leader>ev :split $MYVIMRC<cr>
nnoremap <leader>sv :source $MYVIMRC<cr> 

" deletes single line when in insert mode
inoremap <c-d> <esc>ddi
" changes a word to upper case when in insert mode
inoremap <c-u> <esc>viwUea
" wraps the word with quotes
nnoremap <leader>" viw<esc>a"<esc>bi"<esc>lel

" cpp abbreviation text
iabbrev icpp #include <bits/stdc++.h><cr>#define ll long long<cr>#define ull unsigned long long<cr>using namespace std;<cr><cr>int main() {<cr><cr>return 0;<cr>}

